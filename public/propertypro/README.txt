Drop the 5 PropertyPro prototype videos here (MP4). The page autoplays them
(muted, looping) and falls back to the poster image until each file exists.

Required filenames:
  search.mp4        -> Search Result Page
  filter.mp4        -> Filter search Result Page
  signup.mp4        -> Sign Up Process
  add-property.mp4  -> Adding a Property Flow
  prototype.mp4     -> Full Interactive Prototype
